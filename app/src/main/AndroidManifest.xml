<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"

    package="com.music.lessons">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:name=".room.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/briefcase"
        android:label="@string/app_name"
        android:roundIcon="@drawable/briefcase"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lessons"
        tools:targetApi="31">
        <activity
            android:name=".Activity.StatisticsActivity"
            android:exported="false" />
        <activity
            android:name=".Activity.InfoStudent"
            android:exported="false" />
        <activity
            android:name=".Activity.AddStudentActivity"
            android:exported="false" />
        <activity
            android:name=".Activity.StudentsActivity"
            android:exported="false" />
        <activity
            android:name=".Activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.lesson.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>


    </application>

</manifest>